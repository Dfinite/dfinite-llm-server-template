# Qwen3-Embedding-0.6B
# ═══════════════════════════════════════════════════════════════
# 0.6B params, 1024dim (Matryoshka 32-1024), max 32768 tokens
# VRAM ~1.2GB
# vLLM ≥ 0.18.0 + --runner pooling
# API: POST /v1/embeddings
# ═══════════════════════════════════════════════════════════════

model:
  name: "qwen3-embedding-0.6b"
  path: "Qwen/Qwen3-Embedding-0.6B"
  description: "Qwen3 Embedding 0.6B — 경량 embedding, 1024dim, Matryoshka"

vllm:
  port: 10073
  runner: "pooling"
  tensor_parallel_size: 1
  max_model_len: 8192
  gpu_memory_utilization: 0.10
  dtype: "auto"
