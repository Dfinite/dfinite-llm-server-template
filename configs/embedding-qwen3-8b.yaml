# Qwen3-Embedding-8B
# ═══════════════════════════════════════════════════════════════
# 8B params, 4096dim (Matryoshka 32-4096), max 32768 tokens
# VRAM ~16GB — MTEB 다국어 1위
# vLLM ≥ 0.18.0 + --runner pooling
# API: POST /v1/embeddings
# ═══════════════════════════════════════════════════════════════

model:
  name: "qwen3-embedding-8b"
  path: "Qwen/Qwen3-Embedding-8B"
  description: "Qwen3 Embedding 8B — MTEB 다국어 1위, 4096dim, Matryoshka"

vllm:
  port: 10073
  runner: "pooling"
  tensor_parallel_size: 1
  max_model_len: 8192
  gpu_memory_utilization: 0.90
  dtype: "auto"
  trust_remote_code: true
