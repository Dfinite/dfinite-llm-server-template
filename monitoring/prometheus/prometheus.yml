# Prometheus — Alloy 통합 수집 기준
# - scrape 주체: Alloy
# - Prometheus 역할: remote_write 수신 + TSDB 저장 + PromQL 조회

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
